version: '3.8'
services:
  web:
    build: .
    container_name: fintalkbot_web
    ports:
      - "5000:5000"
    environment:
      - FLASK_DEBUG=false
      - LOG_LEVEL=INFO
      - CHAT_DB_PATH=/data/fintalk.sqlite3
    volumes:
      - ./data:/data


